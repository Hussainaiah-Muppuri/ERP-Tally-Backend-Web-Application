server:
  port: 9093
spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/erpproject
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true